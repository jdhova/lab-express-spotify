
{{!-- 
{{#each songs}}
  {{this.name}}
{{/each}} --}}



 {{#each songs}}
<p>{{this.name}}</p>

<div class="col-lg-4">
   <audio name="media" controls="">
       <source src="{{this.preview_url}}" type="audio/mpeg">
   </audio>
   {{/each}} 

</div>